---
name: step-c-03b2-offer-generation-enhanced
description: Automatically generate 2-4 offers using LLM, validate against filter, and save complete state to JSON for downstream use
type: automated
nextStepFile: ./c-03c-draft.md
---

<!-- METADATA
   Russian Title: Генерация Офферов с Сохранением JSON (Content Machine Stage 3)
   Estimated Time: ~1 minute
   Trigger: Only if content_type == "demo"
   Input: routine (from c-01), pain_points (from c-02c), selected_angle (from c-03b), offer_filter (from c-03b1)
   Output: generated_offers + complete workflow_state.json
   Stage: Content Machine Stage 3
   JSON State: Updated with generated_offers array
-->

# Step C-03b2: Генерация Офферов с JSON State (Content Machine Stage 3)

**Цель:** Автоматически придумать 2-4 оффера, которые логичны для демонстрируемой рутины + выбранного угла. Сохранить полное состояние в JSON для использования на c-03c.

**Тип:** Automated (система делает сама, пользователь не вводит)

**Триггер:** Только если `content_type == "demo"` (режим [R] в c-01)

---

## Концепция

На этом шаге система **сама генерирует офферы**, комбинируя:
- **Рутину** (то, что вы продемонстрировали в c-01)
- **Боли предпринимателя** (из c-02c)
- **Выбранный угол** (из c-03b)
- **Профиль офферов** (что вы готовы продавать, из c-03b1)

**Результат:** 2-4 конкретных предложения, готовых к вшиванию в черновики постов. Полное состояние сохраняется в `.workflow_state.json`.

---

## Логика Генерации

### Step 1: Load State from c-03b1

**Система загружает существующий state:**

```bash
# Проверка наличия файла
if [ -f ".workflow_state.json" ]; then
  # Load existing state
  STATE=$(cat .workflow_state.json)
  WILLING_OFFERS=$(echo $STATE | jq '.willing_offers')
  CONTENT_TYPE=$(echo $STATE | jq '.content_type')
else
  # ERROR: State not initialized
  echo "⚠️ workflow_state.json not found"
  echo "Step c-03b1 must run first"
  exit 1
fi
```

**Loaded Fields:**
- `willing_offers`: ["training", "setup", "templates", "consulting"]
- `unwilling_offers`: ["full_dev"]
- `session_id`: workflow-{timestamp}
- `current_phase`: Should be "c-03b1-offer-check"

---

### Step 2: Load Additional Input Data

**Система загружает данные от предыдущих шагов:**

```
FROM c-01:
├─ idea_text: "BMAD опросил и сгенерировал 80+ документов за 2 часа"
├─ idea_metadata.visual_context: "Скриншот BMAD интерфейса"
├─ idea_metadata.tools_used: "BMAD, Claude"
└─ idea_metadata.demonstrated_result: "80+ документов за 2 часа"

FROM c-02c:
├─ pain_points_json[selected_angle]: [ "Боль 1", "Боль 2", "Боль 3" ]
└─ (multiple sub-keys for different angles)

FROM c-03b:
├─ selected_angle: "Time Saving"
└─ angle_description: "Экономия времени на документацию"
```

**Update State:**

```json
{
  "current_phase": "c-03b2-offer-generation",
  "idea_text": "...",
  "idea_metadata": {...},
  "pain_points_json": {...},
  "selected_angle": "Time Saving"
}
```

---

### Step 3: Build LLM Prompt

**Система создает промпт для Claude:**

```
Ты — эксперт в генерации продаж-ориентированного контента и продуктовых предложений.

РУТИНА (что демонстрируется):
"{idea_text}"

КОНТЕКСТ РУТИНЫ:
- Инструменты: {tools_used}
- Что видно на скрине: {visual_context}
- Результат: {demonstrated_result}

УГОЛ ПУБЛИКАЦИИ (как это релевантно для читателей):
- Название: {selected_angle}
- Описание: {angle_description}

БОЛИ ПРЕДПРИНИМАТЕЛЕЙ (для этого угла):
{pain_points_list}

ОГРАНИЧЕНИЯ (только эти типы офферов):
{willing_offers}

ЗАДАЧА:
Придумай 2-4 КОНКРЕТНЫХ оффера, которые:
1. ЛОГИЧНЫ для демонстрируемой рутины
2. ЗАКРЫВАЮТ хотя бы одну боль из списка
3. ПРИНАДЛЕЖАТ только к типам из ОГРАНИЧЕНИЙ
4. ИМЕЮТ МЯГКИЙ, НЕКРИКЛИВЫЙ СТИЛЬ

ФОРМАТ ОТВЕТА (JSON только, без комментариев):

[
  {
    "offer_type": "training" | "setup" | "templates" | "consulting",
    "title": "Название (3-5 слов)",
    "description": "Что получит клиент (~100 символов)",
    "cta": "Call-to-Action: мягкий призыв",
    "pain_solved": "Какую боль это закрывает",
    "effort_level": "low" | "medium" | "high",
    "reasoning": "Почему это логично"
  }
]
```

---

### Step 4: Call LLM & Parse Response

**Система вызывает Claude:**

```python
import json
import anthropic

client = anthropic.Anthropic()

try:
    response = client.messages.create(
        model="claude-opus-4-5-20251101",
        max_tokens=2048,
        messages=[
            {"role": "user", "content": prompt}
        ]
    )

    llm_text = response.content[0].text.strip()

    # Try to parse JSON
    try:
        generated_offers = json.loads(llm_text)
        if not isinstance(generated_offers, list):
            raise ValueError("Response is not an array")
    except json.JSONDecodeError as e:
        print(f"⚠️ JSON Parse Error: {e}")
        # Retry once
        if retry_count < 1:
            retry_count += 1
            generated_offers = call_llm_with_retry(prompt)
        else:
            generated_offers = []

except anthropic.APIError as e:
    print(f"⚠️ API Error: {e}")
    generated_offers = []
```

---

### Step 5: Validate Offers

**Система валидирует каждый оффер:**

```python
VALID_OFFER_TYPES = ["training", "setup", "templates", "consulting", "full_dev"]
VALID_EFFORT_LEVELS = ["low", "medium", "high"]

validated_offers = []
warnings = []

for i, offer in enumerate(generated_offers):
    # Check required fields
    if not all(key in offer for key in ["offer_type", "title", "description", "cta", "pain_solved"]):
        warnings.append({
            "type": "MISSING_FIELDS",
            "offer_index": i,
            "detail": "Missing required fields"
        })
        continue

    # Check offer_type is in willing_offers
    if offer["offer_type"] not in willing_offers:
        warnings.append({
            "type": "INVALID_OFFER_TYPE",
            "offer_index": i,
            "detail": f"{offer['offer_type']} not in {willing_offers}"
        })
        continue

    # Check effort_level
    if offer.get("effort_level") not in VALID_EFFORT_LEVELS:
        offer["effort_level"] = "medium"  # Default

    # Add unique offer_id
    offer["offer_id"] = f"offer-{len(validated_offers) + 1}"
    offer["created_at"] = datetime.now().isoformat()

    validated_offers.append(offer)

if len(validated_offers) < 2:
    warnings.append({
        "type": "TOO_FEW_OFFERS",
        "detail": f"Only {len(validated_offers)} valid offers, expected 2-4"
    })
```

---

### Step 6: Update & Save State

**Система обновляет workflow_state.json:**

```json
{
  "session_id": "workflow-2026-01-30T10-30-00Z",
  "current_phase": "c-03b2-offer-generation",
  "session_mode": "demo",
  "content_type": "demo",
  "selected_idea_id": "idea-123",
  "idea_text": "BMAD опросил и сгенерировал 80+ документов за 2 часа",
  "idea_metadata": {
    "visual_context": "Скриншот BMAD интерфейса с 80+ документами",
    "tools_used": "BMAD, Claude",
    "demonstrated_result": "80+ документов за 2 часа"
  },
  "selected_angle": "Time Saving",
  "pain_points": ["Боль 1", "Боль 2", "Боль 3"],
  "willing_offers": ["training", "setup", "templates", "consulting"],
  "unwilling_offers": ["full_dev"],
  "generated_offers": [
    {
      "offer_id": "offer-1",
      "offer_type": "training",
      "title": "Обучение BMAD для Документации",
      "description": "Научу вас генерировать 80+ документов за 2 часа, как в демо. Вы получите готовый workflow, шаблоны и полное понимание процесса.",
      "cta": "Напишите в личку — обсудим формат обучения.",
      "pain_solved": "Медленная обработка большого объёма документов",
      "effort_level": "medium",
      "reasoning": "Рутина показывает реальный инструмент, результат, боль. Обучение масштабируется.",
      "created_at": "2026-01-30T10:35:00Z"
    },
    {
      "offer_id": "offer-2",
      "offer_type": "setup",
      "title": "Setup BMAD-Процесса за 3 Дня",
      "description": "Спроектирую и разверну workflow для вашей документации с минимальным участием. Адаптация под вашу задачу.",
      "cta": "Дайте мне юзкейс — покажу, как это может работать.",
      "pain_solved": "Нет описанных процессов → бизнес не продать",
      "effort_level": "high",
      "reasoning": "Setup решает две боли: процессы документируются, люди заменяются.",
      "created_at": "2026-01-30T10:35:00Z"
    }
  ],
  "generated_offers_count": 2,
  "offers_generated_at": "2026-01-30T10:35:00Z",
  "warnings": [],
  "errors": [],
  "state_last_updated_at": "2026-01-30T10:35:00Z",
  "state_expires_at": "2026-01-30T18:30:00Z",
  "state_file_path": "./.workflow_state.json"
}
```

**Save to disk:**

```bash
# Minified for file size
cat > .workflow_state.json << 'EOF'
{complete JSON as above}
EOF

# Verify file was created
if [ -f ".workflow_state.json" ]; then
  echo "✅ State saved to .workflow_state.json"
else
  echo "❌ ERROR: Failed to save state"
  exit 1
fi
```

---

## User Output

**Система показывает:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ ОФФЕРЫ СГЕНЕРИРОВАНЫ АВТОМАТИЧЕСКИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

На основе вашей рутины и выбранного угла система придумала
оффера, которые закрывают реальные боли предпринимателей.

Эти офферы будут автоматически вшиты в черновики постов
на шаге c-03c (в PAS, Hook-Story-Offer, Show Your Work).

Состояние сохранено в: .workflow_state.json

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  ОБУЧЕНИЕ BMAD ДЛЯ ДОКУМЕНТАЦИИ (training)

    Описание:
    "Научу вас генерировать 80+ документов за 2 часа, как в демо.
     Вы получите готовый workflow, шаблоны и полное понимание процесса."

    Закрывает боль:
    "Медленная обработка большого объёма документов"

    Call-to-Action:
    "Напишите в личку — обсудим формат обучения."

    Усилия: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2️⃣  SETUP BMAD-ПРОЦЕССА ЗА 3 ДНЯ (setup)

    Описание:
    "Спроектирую и разверну workflow, который будет генерировать
     вашу документацию с минимальным участием. Адаптация под вашу задачу."

    Закрывает боль:
    "Нет описанных процессов → бизнес не продать"

    Call-to-Action:
    "Дайте мне юзкейс компании — покажу, как это может работать."

    Усилия: high

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 На следующем шаге эти офферы будут вшиты в черновики.
   Вы сможете отредактировать или заменить офферы при написании.

[Продолжить к написанию черновиков →]
```

---

## Error Handling

### Scenario 1: No State File

```
⚠️ ERROR: workflow_state.json not found

Step c-03b1 (Offer Filter Setup) must run first.
Run this step again after c-03b1 completes.

[Back to c-03b1]
```

### Scenario 2: JSON Parse Error

```
⚠️ LLM вернул непарсируемый JSON

Система пытается парсить... [Retry 1/2]

(Если повтор не помогает → Continue with empty offers array)
```

### Scenario 3: Validation Failures

```
⚠️ Система сгенерировала офферы за пределами вашего профиля.

Сгенерировано: full_dev (разработка)
Ваш профиль: training, setup, templates, consulting

Попробуем ещё раз с упором на ваш профиль... [Retry 1/2]
```

### Scenario 4: Too Few Valid Offers

```
⚠️ После валидации осталось только 1 оффер (нужно 2-4).

Система продолжит со спасенными офферами.
На шаге c-03c вы сможете вручную добавить дополнительные офферы.
```

---

## State Lifecycle

### File Management

- **Location:** `./.workflow_state.json` (root of project)
- **Created:** c-03b1 (after offer filter setup)
- **Updated:** c-03b2 (after offer generation)
- **Further Updated:** c-03c (after draft selection)
- **Expires:** 8 hours after creation (auto-cleanup)

### Cleanup

```bash
# Auto-cleanup (runs at session end or next session start)
if [ -f ".workflow_state.json" ]; then
  EXPIRES=$(cat .workflow_state.json | jq -r '.state_expires_at')
  EXPIRES_EPOCH=$(date -d "$EXPIRES" +%s)
  NOW_EPOCH=$(date +%s)

  if [ $NOW_EPOCH -gt $EXPIRES_EPOCH ]; then
    rm .workflow_state.json
    echo "🗑️  Old workflow state cleaned up"
  fi
fi
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│ INPUT: c-03b1 Offer Filter Setup                        │
│ ├─ willing_offers: ["training", "setup", ...]           │
│ ├─ unwilling_offers: ["full_dev"]                       │
│ └─ session_id, offer_filter_configured                  │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│ INPUT: c-01 (Demo Routine)                              │
│ ├─ idea_text, tools_used, visual_context                │
│ └─ demonstrated_result                                  │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│ INPUT: c-02c (Pain Points)                              │
│ ├─ pain_points_json[selected_angle]                     │
│ └─ List of 3-5 pain points                              │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│ INPUT: c-03b (Selected Angle)                           │
│ ├─ selected_angle: "Time Saving"                        │
│ └─ angle_description                                    │
└──────────────────┬──────────────────────────────────────┘
                   │
    ┌──────────────┴──────────────┐
    │                             │
    ▼                             ▼
┌──────────────────┐  ┌──────────────────┐
│ Build LLM Prompt │  │ Load State from  │
│ with all inputs  │  │ c-03b1           │
└──────────────────┘  └──────────────────┘
         │                     │
         └────────────┬────────┘
                      ▼
            ┌──────────────────┐
            │  Call Claude API │
            │  (max 2 retries) │
            └──────────────────┘
                      │
                      ▼
            ┌──────────────────┐
            │ Parse JSON Array │
            │ (error handling) │
            └──────────────────┘
                      │
                      ▼
            ┌──────────────────┐
            │ Validate Offers: │
            │ ✓ offer_type     │
            │ ✓ in willing     │
            │ ✓ required fields│
            └──────────────────┘
                      │
                      ▼
    ┌─────────────────────────────┐
    │ UPDATE .workflow_state.json  │
    │ ├─ generated_offers[]       │
    │ ├─ generated_offers_count   │
    │ ├─ offers_generated_at      │
    │ ├─ warnings[], errors[]     │
    │ └─ state_last_updated_at    │
    └─────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────┐
        │ Display Results to User │
        │ [Continue to c-03c →]   │
        └─────────────────────────┘
```

---

## What's Next?

На следующем шаге (c-03c):

1. Загрузить `.workflow_state.json`
2. Извлечь `generated_offers` массив
3. Генерировать 3 базовых варианта (DIRECT, STORYTELLING, DATA-DRIVEN)
4. **Генерировать 3 CM варианта:**
   - Вариант 4: PAS (Problem-Agitate-Solution) — с вшитыми болями из `pain_points`
   - Вариант 5: Hook-Story-Offer — с вшитыми офферами из `generated_offers`
   - Вариант 6: Show Your Work — комбинация боли + оффера
5. Очистить state после использования офферов (или сохранить для истории)

---

**nextStepFile:** `./c-03c-draft.md`
